# Use a lightweight official Python image suitable for production
FROM python:3.11-slim

# Set environment variables
ENV PYTHONUNBUFFERED 1
ENV PYTHONDONTWRITEBYTECODE 1

# Set the working directory in the container
WORKDIR /app

# 1. Copy only requirements.txt first to leverage Docker caching.
COPY requirements.txt .

# 2. Install Dependencies, Build Tools, and then CLEAN UP immediately.
# This chained RUN command minimizes layers and drastically reduces the final image size
# by removing the build tools (like build-essential) after they are no longer needed.
RUN apt-get update && \
    # Install build tools needed for packages like lxml, numpy, etc.
    apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/* && \
    \
    # Install Python dependencies with a generous timeout (300 seconds)
    pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir --timeout 300 -r requirements.txt && \
    \
    # CLEAN UP: Uninstall the build tools to shrink the final image size
    apt-get purge -y --auto-remove build-essential && \
    rm -rf /var/lib/apt/lists/*

# 3. Copy the rest of the application code
COPY . .

# 4. Define the command to run your application
# The host/port are picked up automatically by Railway.
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "$PORT"]
